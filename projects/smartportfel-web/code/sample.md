# Kod: smartportfel-web

Ten folder jest ignorowany przez git (poza tym plikiem). Sklonuj tu repozytorium aplikacji, żeby asystent mógł je analizować:

```bash
git clone https://github.com/fisieek/smartportfel-web-open-source.git projects/smartportfel-web/code/app
```

Repozytorium: https://github.com/fisieek/smartportfel-web-open-source

Zacznij od `README.md`, `apps/server/prisma/schema.prisma` (model danych) i `apps/server/src/routes/` (lista API).
